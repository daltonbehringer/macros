NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_STYTCH_PUBLIC_TOKEN=

# Sentry (optional locally — leave empty to no-op the SDK)
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_DSN=
SENTRY_ENV=
# Build-time only — set on Vercel for source-map upload, not local
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=
